"""Cross-cutting application concerns: settings, logging, and future security utilities.

Nothing in this package touches the database or HTTP layer directly — it's the
foundation everything else (db, api, services) is built on.
"""
