"""Future home of background/scheduled jobs (e.g. nightly scheme/opportunity
re-matching, forecast generation, digest notifications). No scheduler
library is wired up yet — add it here (e.g. APScheduler) once a first job
exists.
"""
