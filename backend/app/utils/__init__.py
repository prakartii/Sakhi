"""Small, stateless helpers shared across layers (formatting, parsing, etc.)
that don't belong to any one feature. Empty until a concrete need arises —
deliberately not pre-populated with speculative helpers.
"""
